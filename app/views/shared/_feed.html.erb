<div class="col-md-8">
  <%= link_to camp_path(@camp) do %>
    <%= image_tag 'color_run.png', id: "color_run"%>
  <% end %>
<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>

  <%= will_paginate @feed_items %>
<% end %>
</div>
