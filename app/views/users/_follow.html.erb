<!-- rendered by | users/show > users/_follow_form -->

<%= form_for(current_user.active_relationships.new, remote: true) do |f| %>
  <div>
    <%= hidden_field_tag :followed_id, @user.id %>
    <%= f.submit "Follow", class: "btn btn-primary"%>
  </div>
<% end %>
