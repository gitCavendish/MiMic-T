<li>
  <%= image_tag user.icon, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && current_user != user%>
    <%= link_to "Delete", user_path(user), method: :delete, data: {confirm: "Are you sure?"}, class: "delete_user btn btn-sm btn-default"%>
  <% end %>
</li>
